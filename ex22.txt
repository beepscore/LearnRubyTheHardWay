# hash, pound, number sign, octothorpe. Starts a comment.
#! shebang
def define function
end end of def block
% modulus. a%b ==  remainder after division a/b
puts prints a line with a CR.
print prints a line, no CR
"#{my_variable}" string value of my_variable
